what is Set?
ANS:
Set is a collection of Unique Elements. OR Set objects are collectios of values.
If duplicates values are added in the Set, it eliminates it, it make sure each element should be present at once.

Let's see some examples
let array = [1, 2, 3, 3];
let set = new Set(array); // Will have [1, 2, 3]
console.log(typeof set +" "+ set.size);

// create a set
const setCollection = new Set();
// add elements to set
setCollection.add(1);
setCollection.add('a');
setCollection.add({x:1, y:2});
setCollection.add([2,3]);
console.log(setCollection);  // { 1, 'a', { x: 1, y: 2 }, [ 2, 3 ] }

for(let item of set1) console.log(item);
// To iterate set items
for(let item of set) console.log(item); 
/* will print 
1
2
3 
*/

// check if a particular item is present or not
if(set.has(1)) return true  
